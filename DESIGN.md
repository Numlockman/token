# データ構造と処理仕様 v1.1

## 未確定事項の初期決定

| 項目 | 決定 |
|---|---|
| 30桁 | from 8 + to 8 + amount 6 + prevHash 4 + nonce 4 |
| ID | 8桁の文字列。00000000はシステム専用 |
| ID割当案 | 学年2桁＋クラス2桁＋番号4桁。例: 03・02・0017→03020017。来場者は99＋連番6桁。運営側で重複させず配布 |
| 金額 | 内部単位0.1 Coin。送金は0.1〜99999.9 Coin |
| 手数料 | 送金1件につき2単位=0.2 Coin |
| Hash | 4桁、0000〜9999 |
| 判定上限 | 課題ごとに自動決定。Hash < difficulty |
| 目標成功率 | 初期1/3、設定で1/4 |
| 採掘報酬 | 初期10単位=1.0 Coin |
| 取引 | 1課題・1ブロックにつき送金0件または1件 |
| Genesis | index 0、hash 0000。報酬・送金なし、最初の起動時に記録 |

ID生成案は配布のための規則です。アプリは特定の学校・学年・クラスの範囲を強制せず、8桁IDを受け入れます。

## ハッシュの計算（新規ブロック: version 2）

30桁を5桁ずつA〜Fに分ける。先頭の0は数値計算時に無視する。

```text
X = (A + C + E + F) mod 1000
Y = (B + D + E + F) mod 90 + 10
Hash = (X × Y) mod 10000
```

Xは0〜999、Yは10〜99。掛け算は最大3桁×2桁を1回だけ。「mod 1000/10000」は下3桁/下4桁を取り、mod 90は90で割った余りを取る。

例: `00347 00052 00000 00000 00000 00000` → X=347、Y=62 → 347×62=21514 → Hash=1514。

Nonceが含まれるFをX・Y両方に加える。暗号学的な一方向性・改ざん耐性はなく、数学的な近道を見つけることもできる教材用の式。

報酬のみの課題ではfromを00000000、toを採掘者ID、amountを000000にする。

## 成功率と自動ターゲット

設定は `successDenominator:3`（約1/3）が初期値。4（約1/4）へ変更可能。課題発行時に次を実行する。

1. 課題の取引・前Hashを固定し、Nonce全10000通りのHash分布を内部計算。
2. 整数ターゲットT=1〜10000について `Hash < T` の成功数を数える。
3. 成功数が10000/分母に最も近いTを選ぶ。同程度なら小さいTを採用。
4. 課題に `difficulty`（T）、`successCount`、`successDenominator` を記録して固定。

同じHashになるNonceがあるため、成功率をぴったり1/3や1/4にできない場合がある。実際の成功数/10000を表示・印刷する。正解Nonceの配列は保持・表示しない。

これは全Nonceから一様に選んだ場合の成功率。連番探索で3〜4回以内の成功を保証しない。過去ブロックの読み込み時にも自動ターゲットと成功数を再計算して検証する。

## 旧台帳との互換性

Genesisは従来のversion 1のまま。新規採掘ブロックはversion 2。

旧version 1は `S=(A+3B+5C+7D+9E+11F) mod 10000`、`Hash=floor(S²/100) mod 10000` で検証する。旧ブロックのHash・報酬・残高は変更しない。旧台帳から起動したときは報酬を引き継ぎ、新しい目標成功率を1/3に設定する。version 2以降へのversion 1の追記は拒否する。

### ハッシュに含まれない情報

30桁の制約から、送金ブロックの採掘者ID・報酬・判定上限・ブロック番号はこのハッシュの入力外（報酬のみのブロックではtoが採掘者ID）。これらも台帳に記録し形式・残高などは検証するが、ファイルを直接編集する人による改ざんは防げない。送信元のデジタル署名もない。スタッフ管理のPC1台で動かす展示用の中央管理台帳であり、本物の分散型暗号通貨の合意・認証を再現するものではない。

## ブロック例

金額は整数単位。次はフィールドを示す構造例で、Nonce・Hash・ターゲット・成功数の有効な組合せを示すものではない。

```json
{
  "version": 2,
  "index": 1,
  "type": "transfer",
  "from": "03020017",
  "to": "03020018",
  "amount": 20,
  "fee": 2,
  "miner": "99000001",
  "difficulty": 3200,
  "successDenominator": 3,
  "successCount": 3333,
  "reward": 10,
  "prevHash": "0000",
  "nonce": "3817",
  "hash": "0017"
}
```

実際の初回ブロックは残高不足を避けるため報酬のみで採掘する。

```json
{"version":1,"index":0,"type":"genesis","hash":"0000","prevHash":"0000"}
```

bankはObject.create(null)で作るID→整数残高の辞書。ブロックの金額・報酬・手数料も整数で記録し、画面表示時のみ10で割る。

## 確定処理

1. 課題発行: ID・送金額・手数料込み残高を検証。現在のprevHash・報酬・目標成功率・採掘者を固定し、全Nonceからターゲットを決定。
2. 課題ごとにランダムな内部トークンを付ける。これは課題の取り違え防止用で、PoW計算には使用しない。
3. 提出: トークン一致、Nonce/Hash形式、再計算、成功条件、残高を再検証。
4. ブロックのJSON文字列＋改行をファイル末尾に書き込み。短い書き込みは残りを繰り返す。
5. fsyncとcloseの成功を確認。
6. bankを更新し、確定ブロックをメモリーに追加。課題を破棄して画面更新。

送信元・送信先・採掘者が同一IDでも、送金・手数料・報酬を順に精算する。事前に差分合算後の残高が安全な整数範囲内か検証する。

Electronのメインプロセスで同期的に確定するので同時提出は直列化される。最初の提出成功で課題は消え、二度目は拒否される。アプリも同じユーザーで二重起動しない。

## 復旧処理

改行まで完了したレコードを先頭から検証して再生する。途中のJSON・番号・前Hash・PoW・金額・残高などが不正なら、その行を示して起動停止。末尾が未完了の場合、正常な接頭部分を検証できたときだけ原本を別名保存し、末尾を削除する。復旧で既存の正常ブロックを書き換えない。

保存に失敗した場合、メモリーの残高は更新せず以降の追記を禁止する。OS側には完全な行が残っている可能性があるため、再起動時に正式な台帳を読み直して状態を確定する。

## 確認状況

Node.js v24.19.0で自動テスト14件に合格。

- 30桁と手計算の既知結果、ID・金額の検証
- 新規発行→送金→再起動で全残高復元、通貨量の保存
- 残高不足・誤答・古い課題・二重確定の拒否
- 送受信者・採掘者が同一の精算
- 課題中の設定固定、過去報酬の保持
- 未完了末尾の退避・復旧・続けて追記
- 最終改行のない完全JSONの未確定扱い
- 台帳途中の破損時の停止と原本保持
- fsync失敗時のbank未更新・追記禁止
- 部分書き込みの繰り返し
- 100回採掘後の整数残高・再起動復元

未確認: Electron実起動、Windows exeのビルド・起動、画面レイアウトと印刷の実機確認、実機の電源断。これらは納品ソースからWindows環境で実施する必要がある。

追加検証: 複数課題の全Nonceを独立計算して両目標の最適ターゲットを確認、判定境界・ターゲット不一致の拒否、旧台帳を保持した新方式への移行と再起動復元。
