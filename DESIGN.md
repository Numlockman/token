# データ構造と処理仕様 v1

## 未確定事項の初期決定

| 項目 | 決定 |
|---|---|
| 30桁 | from 8 + to 8 + amount 6 + prevHash 4 + nonce 4 |
| ID | 8桁の文字列。00000000はシステム専用 |
| ID割当案 | 学年2桁＋クラス2桁＋番号4桁。例: 03・02・0017→03020017。来場者は99＋連番6桁。運営側で重複させず配布 |
| 金額 | 内部単位0.1 Coin。送金は0.1〜99999.9 Coin |
| 手数料 | 送金1件につき2単位=0.2 Coin |
| Hash | 4桁、0000〜9999 |
| 判定上限 | 初期500。Hash < difficulty。大きいほど簡単 |
| 採掘報酬 | 初期10単位=1.0 Coin |
| 取引 | 1課題・1ブロックにつき送金0件または1件 |
| Genesis | index 0、hash 0000。報酬・送金なし、最初の起動時に記録 |

ID生成案は配布のための規則です。アプリは特定の学校・学年・クラスの範囲を強制せず、8桁IDを受け入れます。

## ハッシュの計算

30桁を5桁ずつA〜Fに分ける。先頭の0を含むブロックは普通の整数として計算する。

```text
S = (A + 3B + 5C + 7D + 9E + 11F) mod 10000
Hash = floor(S × S / 100) mod 10000
```

「mod 10000」は下4桁だけを取る操作。「floor(…/100)」は整数の末尾2桁を消す操作。

例: A=01234、他が00000の場合、S=1234、S²=1522756。末尾2桁を消して15227、その下4桁は5227。

Nonceを1増やすとSは11変わるが、平方・桁の切り取りによりHashの差は一定にならない。暗号学的な一方向性・改ざん耐性はない。数学的な近道を探すことも可能な教材用の式。

Nonceの0000〜9999でSの全10000通りを一巡する。初期判定上限500では成功Nonceが694個（6.94%）。一様ランダムに独立に試すなら平均約14.4回。ハッシュは一様分布ではないため500/10000=5%とはならず、連番探索の所要回数も一定ではない。

報酬のみの課題ではfromを00000000、toを採掘者ID、amountを000000にする。toは報酬の受取先を表す。これにより最初の課題が全桁0になることを避ける。

### ハッシュに含まれない情報

30桁の制約から、送金ブロックの採掘者ID・報酬・判定上限・ブロック番号はこのハッシュの入力外（報酬のみのブロックではtoが採掘者ID）。これらも台帳に記録し形式・残高などは検証するが、ファイルを直接編集する人による改ざんは防げない。送信元のデジタル署名もない。スタッフ管理のPC1台で動かす展示用の中央管理台帳であり、本物の分散型暗号通貨の合意・認証を再現するものではない。

## ブロック例

金額は整数単位。次はフィールドを示す構造例で、NonceとHashの有効な組合せを示すものではない。

```json
{
  "version": 1,
  "index": 1,
  "type": "transfer",
  "from": "03020017",
  "to": "03020018",
  "amount": 20,
  "fee": 2,
  "miner": "99000001",
  "difficulty": 500,
  "reward": 10,
  "prevHash": "0000",
  "nonce": "3817",
  "hash": "0017"
}
```

実際の初回ブロックは残高不足を避けるため報酬のみで採掘する。

```json
{"version":1,"index":0,"type":"genesis","hash":"0000","prevHash":"0000"}
```

bankはObject.create(null)で作るID→整数残高の辞書。ブロックの金額・報酬・手数料も整数で記録し、画面表示時のみ10で割る。

## 確定処理

1. 課題発行: ID・送金額・手数料込み残高を検証。現在のprevHash・報酬・判定上限・採掘者を固定。
2. 課題ごとにランダムな内部トークンを付ける。これは課題の取り違え防止用で、PoW計算には使用しない。
3. 提出: トークン一致、Nonce/Hash形式、再計算、成功条件、残高を再検証。
4. ブロックのJSON文字列＋改行をファイル末尾に書き込み。短い書き込みは残りを繰り返す。
5. fsyncとcloseの成功を確認。
6. bankを更新し、確定ブロックをメモリーに追加。課題を破棄して画面更新。

送信元・送信先・採掘者が同一IDでも、送金・手数料・報酬を順に精算する。事前に差分合算後の残高が安全な整数範囲内か検証する。

Electronのメインプロセスで同期的に確定するので同時提出は直列化される。最初の提出成功で課題は消え、二度目は拒否される。アプリも同じユーザーで二重起動しない。

## 復旧処理

改行まで完了したレコードを先頭から検証して再生する。途中のJSON・番号・前Hash・PoW・金額・残高などが不正なら、その行を示して起動停止。末尾が未完了の場合、正常な接頭部分を検証できたときだけ原本を別名保存し、末尾を削除する。復旧で既存の正常ブロックを書き換えない。

保存に失敗した場合、メモリーの残高は更新せず以降の追記を禁止する。OS側には完全な行が残っている可能性があるため、再起動時に正式な台帳を読み直して状態を確定する。

## 確認状況

Node.js v24.19.0で自動テスト11件に合格。

- 30桁と手計算の既知結果、ID・金額の検証
- 新規発行→送金→再起動で全残高復元、通貨量の保存
- 残高不足・誤答・古い課題・二重確定の拒否
- 送受信者・採掘者が同一の精算
- 課題中の設定固定、過去報酬の保持
- 未完了末尾の退避・復旧・続けて追記
- 最終改行のない完全JSONの未確定扱い
- 台帳途中の破損時の停止と原本保持
- fsync失敗時のbank未更新・追記禁止
- 部分書き込みの繰り返し
- 100回採掘後の整数残高・再起動復元

未確認: Electron実起動、Windows exeのビルド・起動、画面レイアウトと印刷の実機確認、実機の電源断。これらは納品ソースからWindows環境で実施する必要がある。
