# データ構造と処理仕様 v1.2

## 35桁の統一レイアウト（新規ブロックversion 3）

| 位置（1始まり） | フィールド | 桁数 |
|---|---|---:|
| 1〜8 | 採掘者ID | 8 |
| 9〜12 | Nonce | 4 |
| 13〜20 | 送信者ID | 8 |
| 21〜23 | 送金額（0.1 Coin単位） | 3 |
| 24〜31 | 送信先ID | 8 |
| 32〜35 | 前Hash | 4 |

合計35桁。先頭から5桁×7組に区切ってA〜Gにする。フィールド境界とは一致せず、NonceはBの末尾2桁とCの先頭2桁に分かれる。

採掘のみも同じ並び。送信者・送信先は `00000000`、送金額は `000`。報酬はブロックの `reward` から採掘者IDに加算する。これにより、3人の採掘のみの問題でも送信先は変わらず、採掘者IDだけが異なる。

IDは8桁文字列。`00000000` はシステム用。割当例は学年2桁＋クラス2桁＋番号4桁、来場者は99＋連番6桁。割当方法自体は強制しない。

## 新ハッシュ

A〜Gをそれぞれ「上2桁＋下3桁」に足し合わせ、a〜gにする。各5桁の全桁を計算に含め、Nonceの4桁も使用する。

```text
a = floor(A / 1000) + A mod 1000 （b〜gも同様）
X = (a + c + e + g) mod 1000
Y = (b + d + f + g) mod 90 + 10
Hash = (X × Y) mod 10000
```

各5桁の分解は筆記上の桁分けで済み、実際の割り算は90で割った余りを求める1回。Xは0〜999、Yは10〜99なので、掛け算は最大3桁×2桁を1回。

検算例（フィールドが有効な取引という意味ではない）:

```text
A     B     C     D     E     F     G
00347 00052 00000 00000 00000 00000 00000

a=347、b=52、他は0
X=347、Y=52+10=62
347×62=21514 → Hash=1514
```

この式には衝突や数学的な近道がある。教材用であり、暗号学的な安全性・改ざん防止を保証しない。採掘者ID・Nonce・送信者・送金額・送信先・前Hashは入力に含むが、報酬・手数料・目標成功率・ターゲット・ブロック番号は入力外。これらは台帳再生時に規則を検証するが、悪意ある直接編集に耐える認証はない。

## ターゲットと成功率

初期 `successDenominator=3`。4も選択可能。課題ごとにNonce全10000通りを走査し、Hashのヒストグラムを作る。

`difficulty=T`（1〜10000）について成功条件は厳密に `Hash < T`。成功数と10000/分母の差を最小化するTを選択。同率なら小さいT。課題に `difficulty` と `successCount` を保存し、画面・印刷用紙に表示する。正解Nonce一覧は保存・表示しない。

競争参加者は同じ目標成功率だが、採掘者IDによる分布差を補正するため各人のTは異なる場合がある。35桁の固定部分は採掘者IDのみ異なる。

これは全Nonceから一様に選ぶ場合の成功割合。3〜4回以内の成功は保証せず、Hashの重複により目標割合にぴったり一致しない場合もある。

## 送金依頼とラウンド

- `requests`: メモリー上の送金依頼配列。UUID、表示番号、from、to、amountを持つ。
- `round`: 同時進行する1ラウンドのUUIDと対象requestId。採掘のみはrequestId=null。
- `pending`: そのラウンドの計算課題、最大3件。各課題は採掘者IDと内部トークンを持つ。

### 掲示

送信者・送信先ID、送金額0.1〜99.9 Coin、手数料込み残高を検証して付箋を追加。掲示だけでは残高は移動・予約しない。複数依頼の合計が現在の残高を超える場合も掲示できるが、発行・確定時に再検証する。

### 問題発行

付箋をクリックして対象依頼を選び、採掘者IDを入力する。バックエンドは依頼IDから正式な依頼内容を取り出す。画面から送金内容を上書きできない。

最初の1人で取引、ブロック番号、前Hash、報酬、手数料、目標成功率を固定。2〜3人目は同じ対象依頼のみ許可し、採掘者IDの重複・4人目・他の依頼・採掘のみとの混在は拒否する。課題発行後の設定変更も拒否する。

ラウンド中に新しい依頼を掲示することはできるが、別の依頼を採掘することはできない。

### 先着確定

1. 回答の課題トークンが現在のpendingにあるか確認。
2. Nonce・Hashの4桁形式、再計算、成功条件、ターゲット・成功数、残高を検証。
3. JSONLへ追記してfsyncし、残高を更新。
4. 対象の付箋を削除し、pending全件とroundを破棄。

「先着」は1台のPCが最初に正解として検証し、保存を完了した回答。処理はメインプロセス内で同期的に直列化される。別の回答が続けて届いても、トークンが消えているため無効。正解者にだけ報酬と手数料が入り、取引も1回だけ実行される。

誤答では誰の問題も失効させない。保存失敗時は付箋やラウンドを確定扱いにせず、以後の書き込み・操作を止める。再起動時の正式な台帳を基準として残高を復旧する。

### 取消・失効

取消はラウンド全員に対して行う。付箋は残り、全問題のトークンを失効させる。再発行して同じ計算結果になっても、古いトークンでの回答は拒否。用紙には識別番号を印刷する。

採掘中の付箋の削除は拒否する。ラウンドを取り消してから取り下げる。

## ブロックの構造

以下はフィールドの構造例。Nonce・Hash・ターゲット・成功数が有効な組合せであることは示さない。

```json
{
  "version": 3,
  "index": 10,
  "type": "transfer",
  "miner": "99000001",
  "from": "03020017",
  "to": "03020018",
  "amount": 20,
  "fee": 2,
  "reward": 10,
  "successDenominator": 3,
  "difficulty": 3200,
  "successCount": 3333,
  "prevHash": "0042",
  "nonce": "0056",
  "hash": "1514"
}
```

amount=20は2.0 Coin。新規送金はamount<=999。残高・報酬は3桁に制限しない。初期報酬10単位=1.0 Coin、手数料2単位=0.2 Coin。

Genesisは従来通り:

```json
{"version":1,"index":0,"type":"genesis","hash":"0000","prevHash":"0000"}
```

## 永続化と旧台帳

正式な状態は `blockchain.jsonl`。bankは整数残高の辞書として各ブロックから再構築する。

旧ブロックを変更せず、versionに応じた処理で再生する。

| version | 入力 | ハッシュ |
|---|---|---|
| 1 | from8 + to8 + amount6 + prevHash4 + nonce4 | 重み付き和の下4桁を二乗し、桁を取り出す |
| 2 | 同上30桁 | X=(A+C+E+F)%1000、Y=(B+D+E+F)%90+10、Hash=X×Y%10000 |
| 3 | 指定順の35桁 | 7組を桁分けして足し、上記の新式 |

旧版の報酬のみブロックはto=miner。新版はto=00000000。新規発行はversion 3のみ。チェーン途中のversionの逆戻りを拒否する。旧版にある99.9 Coin超の送金は旧仕様の上限で読み込む。

v1の最後の報酬は引き継ぎ、目標成功率は初期1/3。v2/v3の最後の確定設定はそのまま引き継ぐ。

JSONLは検証→末尾追記→fsync→bank更新の順。完成した各行を先頭から再検証。途中の破損は原本を保持して起動停止。未完了の末尾は正常な接頭部分を確認した後、原本をバックアップして切り落とす。

付箋・問題・ラウンドはメモリーのみ。終了・クラッシュ・再起動で破棄し、台帳の復旧から未確定依頼を再作成しない。設定変更も次の確定まで永続化されない。

## 確認状況

Node.jsの自動テスト22件に合格。35桁の配置、手計算例、成功率、旧台帳のv1→v2→v3混在復元、送金・手数料、3人の各人が先着する場合、敗者の回答拒否、4人目・重複ID・別取引の拒否、付箋の残高再検証・取消・消費、保存失敗時の未確定保持、台帳破損復旧を確認。

Electron実画面、Windowsのexeビルド・実機起動、印刷レイアウトの実機確認、実際の電源断は未実施。
